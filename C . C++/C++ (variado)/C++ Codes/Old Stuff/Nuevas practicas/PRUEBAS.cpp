/*#include <iostream> //<< >> 
using namespace std;

void MuestraDeArreys(int Arrey[], int Espacio);
int Suma(int Arrey[], int Espacio);

int main(int argc, char *argv)
{
    int acomulador, a,b,c[128];
    for(a=0; a<=128; a++)
    {
        cout << "Entra el numero del acomulador: ";
        cin >> b;
        if(b<0)
        {
            break
        }
        c[a] = b;
}
MuestraDeArreys(Arrey[], Espacio);
*//*
#include <iostream>
using namespace std;

int suma(int);
int main(int argc, char *argcv)
{    int b[2];
     cout << "Ingrese el primer termino: ";
     cin  >> b[0];
     cout << "Ingrese el segundo termino: ";
     cin  >> b[1];
     int r = suma(b[2]);
     cout << "El resultado es: " << r;
     cin.get();
     cin.get();
return 0;
}
int suma(int a)
{
return  a+a;
} */
// arrays example
// more pointers
// array of structures
// pointer to classes example
// multiple inheritance
// dynamic allocation and polymorphism
// reading a complete binary file
#include <iostream>
using namespace std;

int main (int argc, char *argv[])
{
int x;
    for(int i=0; i<100; i++)
{
cout << i * x << "\n" ;
 x++;
}
cin.get();
return 0;
}
